const fs = require('fs'); let c = fs.readFileSync('src/modules/competition/controller/competition.controller.js', 'utf8'); c = c.replace('const { Apinator } = require(\'@apinator/server\');', 'const Pusher = require(\'pusher\');'); c = c.replace('const pusher = new Apinator({', 'const pusher = new Pusher({'); fs.writeFileSync('src/modules/competition/controller/competition.controller.js', c);
